[package]
name = "udp-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true, features = ["rt", "net", "sync", "macros"] }
traffic-server-core = { path = "../traffic-server-core" }
